<!-- 각 항목의 기준·해석은 .claude/docs/pr-checklist.md를 단일 출처로 둔다.
     해당 없는 항목도 비워 두지 말고 N/A로 명시하고 이유를 적는다. -->

## 개요

<!-- 이 PR을 한 줄로 요약 -->

## 변경 사항

<!-- 무엇을 왜 변경했는지. 어떤 레이어/슬라이스를 바꿨는지 실제 파일·컴포넌트·훅 이름으로.
     동작을 바꿨다면 Before → After 로 대비해 적는다. -->

## 선(先)검증

<!-- API/데이터 가정을 어떻게 확인했는지. 실제 응답 값을 인용한다(추측 금지).
     해당 시 `.claude/scripts/` 사용. 해당 없으면 N/A -->

## 문서 정합성

<!-- 관련 문서(.claude/rules·docs)를 함께 수정했는지, 안 했다면 왜 필요 없었는지 -->

## 전략 영향 평가

<!-- FSD 경계(단방향)·슬라이스 공개 API·데이터 페칭(TanStack Query)·지도 센터 이동 정책과
     충돌하는 부분이 있는지. 사용자 논의가 필요한 결정이 있었는지. 해당 없으면 N/A -->

## 스크린샷

<!-- UI 변경이 있는 경우 -->

## 한계 / 미검증

<!-- 정직한 보고: 수행하지 못한 검증(브라우저 렌더 확인 등)과 수동 검증 방법을 명시 -->

## 체크리스트

- [ ] `pnpm --filter web lint` 통과 (린트·FSD 경계)
- [ ] `pnpm --filter web build` 통과 (타입체크 + 빌드)
- [ ] `pnpm --filter web exec vitest run` 통과 (변경에 대한 테스트 포함)
- [ ] 가정을 실제 API/데이터로 선검증함 (해당 시 `.claude/scripts/` 사용)
- [ ] 작은 커밋 단위로 분리함
